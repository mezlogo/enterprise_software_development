int findFirstZero(char* array, int length);

void resetAllChars(char* array, int length);

long sumOfMulOfTwoArray(int* first, int* second,
						int length);

int sumOfArray(int* array, int length);

int isAscending(int* array, int length);

int isOnlyPositive(int* array, int length);
