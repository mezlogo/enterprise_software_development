#ifndef LINKED_LIST_HANDLER_H
    #define LINKED_LIST_HANDLER_H

    #include "Key.h"
    #include "LinkedList.h"

    char add(LinkedList* root, Key* key);
    char remove(LinkedList* root, Key* key);

#endif
